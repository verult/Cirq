plugins:
  verult-prowbot/Cirq:
  - trigger
  - config-updater

config_updater:
  maps:
    prow/config.yaml:
      name: config
    prow/plugins.yaml:
      name: plugins
    prow/jobs/**/*.yaml:
      name: job-config
